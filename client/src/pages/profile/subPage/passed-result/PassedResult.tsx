import React from "react";

export const PassedResult = (props: any) => {

    const handleOpenSettings = (event: any) => {

    }

    return (
        <div>
            Result
        </div>
    )
}
